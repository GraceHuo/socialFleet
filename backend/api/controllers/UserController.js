/**
 * UserController
 *
 * @description :: Server-side logic for managing users
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */

module.exports = {
	login: function( req, res ) {
    // send a request over to Twitter with temporary token to exchange it
    console.log( 'test' );
  }
};

